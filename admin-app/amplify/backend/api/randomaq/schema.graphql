type Category @model {
  id: ID!
  name: String!
  image: String! 
  description: String!
  questions: [Question] @connection(name: "CategoryQuestion")
}
type Question @model {
  id: ID!
  content: String!
  answer: String!
  blog: Category @connection(name: "CategoryQuestion")
}